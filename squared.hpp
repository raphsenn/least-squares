#ifndef SQUARED_HPP
#define SQUARED_HPP
#include <vector>
#include <tuple>

std::vector<std::tuple<float, float>> squared(std::vector<std::tuple<float, float>> x, float a, float b, float c);

#endif // SQUARED_HPP
