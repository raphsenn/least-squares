#ifndef LINEAR_HPP
#define LINEAR_HPP
#include <vector>
#include <tuple>

std::vector<std::tuple<float, float>> linear(std::vector<std::tuple<float, float>> x, float m, float b);

#endif // LINEAR_HPP
